<?xml version="1.0" encoding="utf-8"?>
<!--
  Privacy boundary (see PLAN.md §0): audio, transcripts, and the ledger NEVER leave
  the device — all extraction is on-device Gemma. INTERNET exists solely for
  explicit, user-approved cloud missions (Antigravity), whose derived-summary
  payload is shown verbatim on the consent sheet before anything is sent.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LaxmiApplication"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.Laxmi"
        android:usesCleartextTraffic="false">

        <!-- Android 12+ hides platform OpenCL/GL native libs unless declared;
             the LiteRT GPU backend needs these to even attempt GPU init. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />
        <uses-native-library android:name="libGLES_mali.so" android:required="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Laxmi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Invisible share target: WhatsApp replies / texts shared into Laxmi. -->
        <activity
            android:name=".ui.ShareReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:label="Laxmi mein daalo">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

    </application>

</manifest>
